# CyberPress Platform - 可视化图标目录

## 🎯 快速导航

- [导航图标](#导航图标) - 12 个
- [功能图标](#功能图标) - 25 个
- [媒体图标](#媒体图标) - 8 个
- [社交图标](#社交图标) - 7 个
- [状态图标](#状态图标) - 10 个
- [赛博图标](#赛博图标) - 8 个
- [装饰图标](#装饰图标) - 6 个
- [主题图标](#主题图标) - 4 个

---

## 🧭 导航图标 (Navigation Icons)

### HomeIcon - 首页
```tsx
import { HomeIcon } from '@/components/icons';

<HomeIcon size={24} className="text-cyber-cyan" />
```
**用途**: 网站首页链接
**尺寸**: 16, 20, 24, 32, 48px
**变体**: outline

---

### BlogIcon - 博客
```tsx
import { BlogIcon } from '@/components/icons';

<BlogIcon size={24} className="text-cyber-cyan" />
```
**用途**: 博客列表页面
**尺寸**: 16, 20, 24, 32, 48px
**变体**: outline

---

### PortfolioIcon - 作品集
```tsx
import { PortfolioIcon } from '@/components/icons';

<PortfolioIcon size={24} className="text-cyber-cyan" />
```
**用途**: 作品展示页面
**尺寸**: 16, 20, 24, 32, 48px
**变体**: outline

---

### AboutIcon - 关于
```tsx
import { AboutIcon } from '@/components/icons';

<AboutIcon size={24} className="text-cyber-cyan" />
```
**用途**: 关于我们页面
**尺寸**: 16, 20, 24, 32, 48px
**变体**: outline

---

### SearchIcon - 搜索
```tsx
import { SearchIcon } from '@/components/icons';

<SearchIcon size={24} className="text-cyber-cyan" />
```
**用途**: 搜索功能
**尺寸**: 16, 20, 24, 32, 48px
**变体**: outline

---

### MenuIcon - 菜单
```tsx
import { MenuIcon } from '@/components/icons';

<MenuIcon size={24} className="text-cyber-cyan" />
```
**用途**: 移动端导航菜单
**尺寸**: 16, 20, 24, 32, 48px
**变体**: outline, 可切换为 X 图标

---

### ArrowLeftIcon - 左箭头
```tsx
import { ArrowLeftIcon } from '@/components/icons';

<ArrowLeftIcon size={24} className="text-cyber-cyan" />
```
**用途**: 上一页/返回
**尺寸**: 16, 20, 24, 32, 48px
**变体**: outline

---

### ArrowRightIcon - 右箭头
```tsx
import { ArrowRightIcon } from '@/components/icons';

<ArrowRightIcon size={24} className="text-cyber-cyan" />
```
**用途**: 下一页/前进
**尺寸**: 16, 20, 24, 32, 48px
**变体**: outline

---

### ChevronUpIcon - 上箭头
```tsx
import { ChevronUpIcon } from '@/components/icons';

<ChevronUpIcon size={24} className="text-cyber-cyan" />
```
**用途**: 返回顶部
**尺寸**: 16, 20, 24, 32, 48px
**变体**: outline

---

### ChevronDownIcon - 下箭头
```tsx
import { ChevronDownIcon } from '@/components/icons';

<ChevronDownIcon size={24} className="text-cyber-cyan" />
```
**用途**: 展开更多
**尺寸**: 16, 20, 24, 32, 48px
**变体**: outline

---

### ExternalLinkIcon - 外部链接
```tsx
import { ExternalLinkIcon } from '@/components/icons';

<ExternalLinkIcon size={24} className="text-cyber-cyan" />
```
**用途**: 外部链接标识
**尺寸**: 16, 20, 24, 32, 48px
**变体**: outline

---

### LocationIcon - 定位
```tsx
import { LocationIcon } from '@/components/icons';

<LocationIcon size={24} className="text-cyber-cyan" />
```
**用途**: 地理位置信息
**尺寸**: 16, 20, 24, 32, 48px
**变体**: outline

---

## ⚙️ 功能图标 (Functional Icons)

### UserIcon - 用户
```tsx
import { UserIcon } from '@/components/icons';

<UserIcon size={24} className="text-cyber-cyan" />
```
**用途**: 用户中心/登录
**尺寸**: 16, 20, 24, 32, 48px

---

### SettingsIcon - 设置
```tsx
import { SettingsIcon } from '@/components/icons';

<SettingsIcon size={24} className="text-cyber-cyan" />
```
**用途**: 系统设置
**尺寸**: 16, 20, 24, 32, 48px

---

### EditIcon - 编辑
```tsx
import { EditIcon } from '@/components/icons';

<EditIcon size={24} className="text-cyber-cyan" />
```
**用途**: 编辑内容
**尺寸**: 16, 20, 24, 32, 48px

---

### TrashIcon - 删除
```tsx
import { TrashIcon } from '@/components/icons';

<TrashIcon size={24} className="text-cyber-red" />
```
**用途**: 删除操作
**尺寸**: 16, 20, 24, 32, 48px
**颜色**: 红色（危险操作）

---

### SaveIcon - 保存
```tsx
import { SaveIcon } from '@/components/icons';

<SaveIcon size={24} className="text-cyber-green" />
```
**用途**: 保存内容
**尺寸**: 16, 20, 24, 32, 48px
**颜色**: 绿色（成功操作）

---

### RefreshIcon - 刷新
```tsx
import { RefreshIcon } from '@/components/icons';

<RefreshIcon size={24} className="text-cyber-cyan" />
```
**用途**: 刷新页面/数据
**尺寸**: 16, 20, 24, 32, 48px
**动画**: 支持旋转动画

---

### CopyIcon - 复制
```tsx
import { CopyIcon } from '@/components/icons';

<CopyIcon size={24} className="text-cyber-cyan" />
```
**用途**: 复制链接/内容
**尺寸**: 16, 20, 24, 32, 48px

---

### BookmarkIcon - 书签
```tsx
import { BookmarkIcon } from '@/components/icons';

// 空心状态
<BookmarkIcon size={24} variant="outline" />

// 实心状态
<BookmarkIcon size={24} variant="solid" className="text-cyber-pink" />
```
**用途**: 收藏内容
**尺寸**: 16, 20, 24, 32, 48px
**变体**: outline, solid

---

### FolderIcon - 文件夹
```tsx
import { FolderIcon } from '@/components/icons';

<FolderIcon size={24} className="text-cyber-cyan" />
```
**用途**: 文件管理
**尺寸**: 16, 20, 24, 32, 48px

---

### FileIcon - 文件
```tsx
import { FileIcon } from '@/components/icons';

<FileIcon size={24} className="text-cyber-cyan" />
```
**用途**: 文件类型标识
**尺寸**: 16, 20, 24, 32, 48px
**变体**: 支持多种文件类型颜色

---

### DownloadIcon - 下载
```tsx
import { DownloadIcon } from '@/components/icons';

<DownloadIcon size={24} className="text-cyber-cyan" />
```
**用途**: 下载文件
**尺寸**: 16, 20, 24, 32, 48px

---

### UploadIcon - 上传
```tsx
import { UploadIcon } from '@/components/icons';

<UploadIcon size={24} className="text-cyber-cyan" />
```
**用途**: 上传文件
**尺寸**: 16, 20, 24, 32, 48px

---

### LockIcon - 锁定
```tsx
import { LockIcon } from '@/components/icons';

<LockIcon size={24} className="text-cyber-orange" />
```
**用途**: 私密内容/权限控制
**尺寸**: 16, 20, 24, 32, 48px

---

### UnlockIcon - 解锁
```tsx
import { UnlockIcon } from '@/components/icons';

<UnlockIcon size={24} className="text-cyber-green" />
```
**用途**: 公开内容
**尺寸**: 16, 20, 24, 32, 48px

---

### EyeIcon - 可见
```tsx
import { EyeIcon } from '@/components/icons';

<EyeIcon size={24} className="text-cyber-cyan" />
```
**用途**: 显示内容/查看
**尺寸**: 16, 20, 24, 32, 48px

---

### EyeOffIcon - 隐藏
```tsx
import { EyeOffIcon } from '@/components/icons';

<EyeOffIcon size={24} className="text-cyber-gray-300" />
```
**用途**: 隐藏内容
**尺寸**: 16, 20, 24, 32, 48px

---

### ShieldIcon - 安全
```tsx
import { ShieldIcon } from '@/components/icons';

<ShieldIcon size={24} className="text-cyber-green" />
```
**用途**: 安全功能/验证
**尺寸**: 16, 20, 24, 32, 48px

---

### BellIcon - 通知
```tsx
import { BellIcon } from '@/components/icons';

<BellIcon size={24} className="text-cyber-cyan" />
```
**用途**: 通知提醒
**尺寸**: 16, 20, 24, 32, 48px
**特殊**: 支持红点提示

---

### CalendarIcon - 日历
```tsx
import { CalendarIcon } from '@/components/icons';

<CalendarIcon size={24} className="text-cyber-cyan" />
```
**用途**: 日期选择/发布时间
**尺寸**: 16, 20, 24, 32, 48px

---

### TagIcon - 标签
```tsx
import { TagIcon } from '@/components/icons';

<TagIcon size={24} className="text-cyber-cyan" />
```
**用途**: 内容分类/标签
**尺寸**: 16, 20, 24, 32, 48px
**颜色**: 支持多种颜色

---

### FilterIcon - 筛选
```tsx
import { FilterIcon } from '@/components/icons';

<FilterIcon size={24} className="text-cyber-cyan" />
```
**用途**: 内容筛选
**尺寸**: 16, 20, 24, 32, 48px

---

### SortIcon - 排序
```tsx
import { SortIcon } from '@/components/icons';

<SortIcon size={24} direction="asc" />
<SortIcon size={24} direction="desc" />
```
**用途**: 内容排序
**尺寸**: 16, 20, 24, 32, 48px
**方向**: asc, desc

---

### TerminalIcon - 终端
```tsx
import { TerminalIcon } from '@/components/icons';

<TerminalIcon size={24} className="text-cyber-cyan" />
```
**用途**: 命令行/代码
**尺寸**: 16, 20, 24, 32, 48px

---

### CodeIcon - 代码
```tsx
import { CodeIcon } from '@/components/icons';

<CodeIcon size={24} className="text-cyber-cyan" />
```
**用途**: 代码展示/开发
**尺寸**: 16, 20, 24, 32, 48px

---

### SyncIcon - 同步
```tsx
import { SyncIcon } from '@/components/icons';

<SyncIcon size={24} className="text-cyber-cyan" />
```
**用途**: 数据同步
**尺寸**: 16, 20, 24, 32, 48px
**动画**: 支持旋转动画

---

## 📱 媒体图标 (Media Icons)

### CameraIcon - 相机
```tsx
import { CameraIcon } from '@/components/icons';

<CameraIcon size={24} className="text-cyber-cyan" />
```
**用途**: 拍照功能
**尺寸**: 16, 20, 24, 32, 48px

---

### VideoIcon - 视频
```tsx
import { VideoIcon } from '@/components/icons';

<VideoIcon size={24} className="text-cyber-cyan" />
```
**用途**: 视频播放/录制
**尺寸**: 16, 20, 24, 32, 48px

---

### MicIcon - 麦克风
```tsx
import { MicIcon } from '@/components/icons';

<MicIcon size={24} className="text-cyber-cyan" />
```
**用途**: 录音功能
**尺寸**: 16, 20, 24, 32, 48px

---

### MusicIcon - 音乐
```tsx
import { MusicIcon } from '@/components/icons';

<MusicIcon size={24} className="text-cyber-cyan" />
```
**用途**: 音频播放
**尺寸**: 16, 20, 24, 32, 48px

---

### ImageIcon - 图片
```tsx
import { ImageIcon } from '@/components/icons';

<ImageIcon size={24} className="text-cyber-cyan" />
```
**用途**: 图片查看/上传
**尺寸**: 16, 20, 24, 32, 48px

---

## 🌐 社交图标 (Social Icons)

### GitHubIcon - GitHub
```tsx
import { GitHubIcon } from '@/components/icons';

<GitHubIcon size={32} className="text-white hover:text-cyber-cyan" />
```
**用途**: GitHub 代码仓库
**尺寸**: 24, 32, 48px
**颜色**: 黑色/白色/青色（悬停）

---

### TwitterIcon - Twitter
```tsx
import { TwitterIcon } from '@/components/icons';

<TwitterIcon size={32} className="text-blue-400 hover:text-blue-300" />
```
**用途**: Twitter 社交媒体
**尺寸**: 24, 32, 48px
**颜色**: 蓝色

---

### LinkedInIcon - LinkedIn
```tsx
import { LinkedInIcon } from '@/components/icons';

<LinkedInIcon size={32} className="text-blue-600 hover:text-blue-500" />
```
**用途**: LinkedIn 职场社交
**尺寸**: 24, 32, 48px
**颜色**: 蓝色

---

### EmailIcon - Email
```tsx
import { EmailIcon } from '@/components/icons';

<EmailIcon size={32} className="text-cyber-cyan" />
```
**用途**: 邮件联系
**尺寸**: 24, 32, 48px
**颜色**: 多色

---

### DiscordIcon - Discord
```tsx
import { DiscordIcon } from '@/components/icons';

<DiscordIcon size={32} className="text-purple-500 hover:text-purple-400" />
```
**用途**: Discord 社区聊天
**尺寸**: 24, 32, 48px
**颜色**: 紫色

---

### YouTubeIcon - YouTube
```tsx
import { YouTubeIcon } from '@/components/icons';

<YouTubeIcon size={32} className="text-red-600 hover:text-red-500" />
```
**用途**: YouTube 视频平台
**尺寸**: 24, 32, 48px
**颜色**: 红色

---

### RSSIcon - RSS
```tsx
import { RSSIcon } from '@/components/icons';

<RSSIcon size={32} className="text-orange-500 hover:text-orange-400" />
```
**用途**: RSS 订阅源
**尺寸**: 24, 32, 48px
**颜色**: 橙色

---

## ✨ 状态图标 (Status Icons)

### LoadingIcon - 加载
```tsx
import { LoadingIcon } from '@/components/icons';

<LoadingIcon size={24} className="text-cyber-cyan animate-spin" />
```
**用途**: 加载中状态
**尺寸**: 16, 20, 24, 32, 48px
**动画**: 旋转动画

---

### ZapIcon - 快速
```tsx
import { ZapIcon } from '@/components/icons';

<ZapIcon size={24} className="text-cyber-yellow" />
```
**用途**: 快速操作/能量
**尺寸**: 16, 20, 24, 32, 48px

---

### CheckIcon - 成功
```tsx
import { CheckIcon } from '@/components/icons';

<CheckIcon size={24} className="text-cyber-green" />
```
**用途**: 操作成功
**尺寸**: 16, 20, 24, 32, 48px

---

### CloseIcon - 关闭
```tsx
import { CloseIcon } from '@/components/icons';

<CloseIcon size={24} className="text-cyber-gray-300 hover:text-cyber-cyan" />
```
**用途**: 关闭/取消
**尺寸**: 16, 20, 24, 32, 48px

---

### WarningIcon - 警告
```tsx
import { WarningIcon } from '@/components/icons';

<WarningIcon size={24} className="text-cyber-yellow" />
```
**用途**: 警告提示
**尺寸**: 16, 20, 24, 32, 48px

---

### ErrorIcon - 错误
```tsx
import { ErrorIcon } from '@/components/icons';

<ErrorIcon size={24} className="text-cyber-red" />
```
**用途**: 错误提示
**尺寸**: 16, 20, 24, 32, 48px

---

### InfoIcon - 信息
```tsx
import { InfoIcon } from '@/components/icons';

<InfoIcon size={24} className="text-cyber-blue" />
```
**用途**: 信息提示
**尺寸**: 16, 20, 24, 32, 48px

---

### HeartIcon - 喜欢
```tsx
import { HeartIcon } from '@/components/icons';

// 空心状态
<HeartIcon size={24} variant="outline" />

// 实心状态
<HeartIcon size={24} variant="solid" className="text-cyber-pink" />
```
**用途**: 点赞/收藏
**尺寸**: 16, 20, 24, 32, 48px
**变体**: outline, solid

---

### StarIcon - 星标
```tsx
import { StarIcon } from '@/components/icons';

// 空心状态
<StarIcon size={24} variant="outline" />

// 实心状态
<StarIcon size={24} variant="solid" className="text-cyber-yellow" />
```
**用途**: 评分/收藏
**尺寸**: 16, 20, 24, 32, 48px
**变体**: outline, solid

---

### OnlineIcon - 在线
```tsx
import { OnlineIcon } from '@/components/icons';

<OnlineIcon size={12} className="text-cyber-green" />
```
**用途**: 在线状态
**尺寸**: 8, 12, 16px

---

### OfflineIcon - 离线
```tsx
import { OfflineIcon } from '@/components/icons';

<OfflineIcon size={12} className="text-cyber-gray-400" />
```
**用途**: 离线状态
**尺寸**: 8, 12, 16px

---

## 🤖 赛博图标 (Cyber Icons)

### ChipIcon - 芯片
```tsx
import { ChipIcon } from '@/components/icons';

<ChipIcon size={32} className="text-cyber-cyan" />
```
**用途**: 硬件/芯片
**尺寸**: 24, 32, 48px
**特效**: 电路纹理

---

### CpuIcon - CPU
```tsx
import { CpuIcon } from '@/components/icons';

<CpuIcon size={32} className="text-cyber-cyan" />
```
**用途**: 处理器
**尺寸**: 24, 32, 48px
**特效**: 发光效果

---

### DatabaseIcon - 数据库
```tsx
import { DatabaseIcon } from '@/components/icons';

<DatabaseIcon size={32} className="text-cyber-cyan" />
```
**用途**: 数据存储
**尺寸**: 24, 32, 48px
**特效**: 全息效果

---

### NetworkIcon - 网络
```tsx
import { NetworkIcon } from '@/components/icons';

<NetworkIcon size={32} className="text-cyber-cyan" />
```
**用途**: 网络连接
**尺寸**: 24, 32, 48px
**特效**: 节点连接

---

### HologramIcon - 全息
```tsx
import { HologramIcon } from '@/components/icons';

<HologramIcon size={32} className="text-cyber-cyan" />
```
**用途**: 全息投影
**尺寸**: 24, 32, 48px
**特效**: 扫描效果

---

### GlitchIcon - 故障
```tsx
import { GlitchIcon } from '@/components/icons';

<GlitchIcon size={32} className="text-cyber-cyan" />
```
**用途**: 故障艺术
**尺寸**: 24, 32, 48px
**特效**: 像素化

---

### MatrixIcon - 矩阵
```tsx
import { MatrixIcon } from '@/components/icons';

<MatrixIcon size={32} className="text-cyber-green" />
```
**用途**: 矩阵效果
**尺寸**: 24, 32, 48px
**特效**: 代码雨

---

### CyberShieldIcon - 赛博盾
```tsx
import { CyberShieldIcon } from '@/components/icons';

<CyberShieldIcon size={32} className="text-cyber-cyan" />
```
**用途**: 安全防护
**尺寸**: 24, 32, 48px
**特效**: 霓虹发光

---

## 🎨 装饰图标 (Decorative Icons)

### SparkleIcon - 星光
```tsx
import { SparkleIcon } from '@/components/icons';

<SparkleIcon size={24} className="text-cyber-cyan" />
```
**用途**: 装饰点缀
**特效**: 闪烁

---

### FlameIcon - 火焰
```tsx
import { FlameIcon } from '@/components/icons';

<FlameIcon size={24} className="text-cyber-orange" />
```
**用途**: 热门/推荐
**特效**: 动态

---

### BoltIcon - 闪电
```tsx
import { BoltIcon } from '@/components/icons';

<BoltIcon size={24} className="text-cyber-yellow" />
```
**用途**: 快速/能量
**特效**: 发光

---

## 🌓 主题图标 (Theme Icons)

### SunIcon - 太阳
```tsx
import { SunIcon } from '@/components/icons';

<SunIcon size={24} className="text-cyber-yellow" />
```
**用途**: 浅色模式
**颜色**: 黄色

---

### MoonIcon - 月亮
```tsx
import { MoonIcon } from '@/components/icons';

<MoonIcon size={24} className="text-cyber-purple" />
```
**用途**: 深色模式
**颜色**: 紫色

---

## 🎯 使用示例

### 导航栏
```tsx
<header className="flex items-center gap-4">
  <MenuIcon size={24} className="text-cyber-cyan" />
  <SearchIcon size={24} className="text-cyber-cyan" />
  <ThemeIcon size={24} />
</header>
```

### 文章卡片
```tsx
<Card>
  <div className="flex items-center gap-4">
    <CalendarIcon size={16} className="text-cyber-gray-300" />
    <TagIcon size={16} className="text-cyber-cyan" />
    <HeartIcon size={16} variant="outline" className="text-cyber-pink" />
    <BookmarkIcon size={16} variant="outline" />
  </div>
</Card>
```

### 社交链接
```tsx
<div className="flex gap-4">
  <GitHubIcon size={32} className="text-white hover:text-cyber-cyan" />
  <TwitterIcon size={32} className="text-blue-400" />
  <LinkedInIcon size={32} className="text-blue-600" />
</div>
```

---

**版本**: v2.0.0
**最后更新**: 2026-03-07
**维护者**: CyberPress AI Design Team
